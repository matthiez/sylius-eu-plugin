{% macro text(product, variant) %}
    {% if variant.preservative == true or product.preservative == true %}
        {{ 'ecolos_sylius_eu_plugin.preservative' | trans }}
    {% endif %}
{% endmacro %}